# 富文本编辑器功能说明

## 新增功能

### 1. 富文本编辑支持
便签内容现在支持富文本格式，所见即所得的编辑体验。

### 2. 格式化工具栏
位于便签标题栏下方的格式化工具栏，包含以下功能：

#### 文本格式
- **粗体 (B)**：使文本加粗显示（快捷键：Ctrl+B）
- **斜体 (I)**：使文本倾斜显示（快捷键：Ctrl+I）
- **下划线 (U)**：为文本添加下划线（快捷键：Ctrl+U）
- **删除线 (S)**：为文本添加删除线效果

#### 字体大小
- 下拉菜单选择字体大小：10, 12, 14, 16, 18, 20, 24
- 默认字体大小：14

#### 任务列表 (☑)
- 点击插入可交互的复选框任务项
- 支持勾选/取消勾选
- 自动保存任务状态

#### 插入图片 (🖼)
- 点击按钮选择图片文件
- 或直接拖放图片到编辑区域
- 支持的图片格式：PNG, JPEG, GIF, BMP, WEBP

### 3. 拖放插入图片
- 支持将图片文件直接拖放到编辑器中
- 图片会自动调整大小（最大 400x400）
- 图片自动保存到 `NoteImages/{便签ID}/` 目录
- 图片随便签内容一起保存

### 4. 内容自动保存
- 使用 RTF 格式保存富文本内容
- 保留所有格式信息（粗体、斜体、下划线、删除线、字体大小等）
- 保存图片和任务列表状态
- 实时自动保存，无需手动操作

## 使用示例

### 格式化文本
1. 选中要格式化的文本
2. 点击工具栏中对应的格式化按钮
3. 再次点击相同按钮可取消格式

### 插入任务列表
1. 将光标定位到要插入任务的位置
2. 点击工具栏中的 ☑ 按钮
3. 输入任务内容
4. 点击复选框标记任务完成

### 插入图片
**方法 1：使用按钮**
1. 点击工具栏中的 🖼 按钮
2. 在文件对话框中选择图片
3. 图片将插入到当前光标位置

**方法 2：拖放**
1. 从文件管理器拖动图片文件
2. 将图片放到编辑器区域
3. 图片自动插入

### 调整字体大小
1. 选中要调整大小的文本
2. 在工具栏的字体大小下拉菜单中选择目标大小
3. 文本大小立即改变

## 技术实现
- 使用 WPF 内置的 `RichTextBox` 控件
- RTF 格式存储富文本内容
- 图片本地存储，嵌入到文档中
- 任务列表使用 `CheckBox` 控件实现
- 自动保存机制，无需担心数据丢失

## 快捷键
- **Ctrl+B**：粗体
- **Ctrl+I**：斜体
- **Ctrl+U**：下划线

## 注意事项
- 所有格式和内容实时保存
- 图片文件存储在应用程序目录的 `NoteImages` 文件夹中
- 每个便签的图片独立存储在各自的子文件夹
- 删除便签不会自动删除图片文件（需手动清理）
