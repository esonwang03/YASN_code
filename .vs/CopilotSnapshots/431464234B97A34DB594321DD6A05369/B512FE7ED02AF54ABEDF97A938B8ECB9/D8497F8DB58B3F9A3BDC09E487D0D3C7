# YASN - 快速参考指南

## ?? 快速开始

1. 运行程序 → 系统托盘出现图标
2. 右键托盘图标 → New Window → 选择层级
3. 开始使用！

## ?? 窗口层级

| 层级 | 颜色 | 行为 | 用途 |
|------|------|------|------|
| **Top** | ?? 绿边框 + ?? 蓝标题 | 始终最上 | 重要提醒、快速访问 |
| **Normal** | ? 灰边框 + ? 灰标题 | 正常层级 | 普通便签 |
| **Bottom** | ?? 红边框 + ?? 红标题 | 始终最下 | 背景信息、桌面装饰 |

## ?? 操作指南

### 创建窗口
- 托盘右键 → New Window → 选择层级
- 主窗口 → 点击按钮创建

### 移动窗口
- 按住左键拖动窗口任意位置

### 调整大小
- 拖动窗口四角

### 切换层级
- 窗口右键 → Window Level → 选择新层级
- 窗口颜色会自动改变

### 关闭窗口
- 点击右上角 X
- 或右键 → Close Window

### 编辑内容
- 直接在窗口中输入文字
- 支持多行、换行

## ?? 常用场景

### 场景 1: 重要待办事项
创建 **Top** 窗口 → 输入任务清单 → 始终显示在所有窗口之上

### 场景 2: 参考资料
创建 **Normal** 窗口 → 粘贴参考内容 → 需要时切到前面

### 场景 3: 桌面便签墙纸
创建 **Bottom** 窗口 → 输入背景信息 → 不干扰其他操作

### 场景 4: 多窗口组织
- Top: 当前任务
- Normal: 临时笔记
- Bottom: 长期参考

## ?? 系统托盘菜单

```
┌─ YASN ─────────────────┐
│ New Window            ?│
│   ├─ Normal Window     │
│   ├─ Top Window        │
│   └─ Bottom Window     │
├───────────────────────│
│ Show Main Window       │
│ Exit                   │
└────────────────────────┘
```

## ?? 右键菜单（窗口）

```
┌─────────────────────┐
│ Window Level       ?│
│   ├─ ? Always on Top│
│   ├─ ? Normal       │
│   └─ ? Always on... │
├─────────────────────│
│ Close Window        │
└─────────────────────┘
```

## ?? 提示与技巧

### 技巧 1: 快速切换层级
不用关闭重建，直接右键切换层级即可

### 技巧 2: 多窗口管理
每个窗口标题显示编号（#1, #2...）便于识别

### 技巧 3: 底层窗口妙用
在底层窗口放置壁纸、灵感墙、常用密码等

### 技巧 4: 隐藏主窗口
主窗口可以隐藏到托盘，只保留浮动窗口

### 技巧 5: 颜色记忆法
- ?? 绿色 = Go (冲！最前面)
- ? 灰色 = 平凡普通
- ?? 红色 = 退后（最下面）

## ?? 注意事项

1. **内容不保存** - 关闭窗口内容会丢失，记得复制重要信息
2. **底层窗口** - 会被其他窗口覆盖，这是正常的
3. **透明窗口** - 半透明设计可能在某些颜色背景下不易阅读
4. **全屏应用** - 全屏游戏/视频可能覆盖顶层窗口

## ?? 故障排除

### 问题：托盘图标看不见
**解决**：检查任务栏右下角隐藏图标区域

### 问题：底层窗口"消失"了
**解决**：窗口仍在，只是被其他窗口完全覆盖。最小化其他窗口即可看到。

### 问题：无法拖动窗口
**解决**：按住鼠标左键在窗口任意位置拖动（不是右键）

### 问题：窗口关不掉
**解决**：点击右上角 X，或右键菜单 → Close Window

### 问题：想完全退出程序
**解决**：托盘图标右键 → Exit

## ?? 快捷键 (计划中)

目前版本不支持快捷键，未来版本将添加：
- 全局热键创建窗口
- 快速切换层级
- 显示/隐藏所有窗口

## ?? 进阶用法

### 多显示器使用
将窗口拖到不同显示器，独立管理每个屏幕的便签

### 工作流集成
配合其他工具（如任务管理、日历）使用浮动窗口做快速笔记

### GTD 方法论
- Top: 今日必做
- Normal: 本周计划
- Bottom: 长期目标

---

**享受使用 YASN！** ??
