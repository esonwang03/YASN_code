# YASN - Yet Another Sticky Notes (Window Manager)

一个基于WPF的系统托盘应用程序，提供统一的浮动窗口组件，支持动态切换窗口层级（顶层/正常/底层）。

## ✨ 核心特性

### 🎨 统一的现代化设计
- **深色主题**: 所有窗口采用一致的深色半透明设计
- **颜色编码**: 通过边框颜色区分窗口层级
  - 🔵 蓝色边框 = 顶层窗口 (始终最上)
  - ⚪ 灰色边框 = 正常窗口 (普通层级)
  - 🔴 红色边框 = 底层窗口 (始终最下)
- **统一标题栏**: 深灰色背景 (#2D2D30)，所有窗口一致
- **高对比度文本**: 浅色文字 (#ECF0F1)，易读清晰

### 🔄 动态层级切换
- 右键菜单实时切换窗口层级
- 边框颜色即时更新
- 标题自动显示当前层级 (▲ Top / ■ Normal / ▼ Bottom)
- 无需关闭重建窗口

### 📝 便签功能
- 内置文本编辑器
- 支持多行文本
- 自动换行
- 滚动支持

### 🖱️ 便捷操作
- 左键拖动移动窗口
- 边角调整窗口大小
- 右键弹出操作菜单
- 不显示在任务栏

## 功能详情

### 系统托盘
- 应用启动后最小化到系统托盘
- 右键托盘图标显示菜单：
  - **新建窗口** - 子菜单选择层级
    - 普通窗口
    - 置顶窗口  
    - 置底窗口
  - **显示主窗口**
  - **退出**
- 双击托盘图标显示主窗口

### 浮动窗口 (FloatingWindow)

#### 三种窗口层级

**1. 顶层窗口 (Top Most)**
- 🔵 蓝色边框 (#3498DB)
- ▲ 图标标识
- 始终显示在所有窗口之上
- 适合: 重要提醒、待办事项

**2. 正常窗口 (Normal)**
- ⚪ 灰色边框 (#95A5A6)
- ■ 图标标识
- 正常的窗口层级
- 适合: 常规笔记、临时记录

**3. 底层窗口 (Bottom Most)**
- 🔴 红色边框 (#E74C3C)
- ▼ 图标标识
- 始终显示在所有窗口之下
- 适合: 背景信息、参考资料
- 使用定时器持续维护底层位置

#### 右键菜单

```
┌──────────────────────┐
│ Window Level       ► │
│  ├ Always on Top     │ ← 切换到顶层
│  ├ Normal            │ ← 切换到正常
│  └ Always on Bottom  │ ← 切换到底层
├──────────────────────┤
│ Close Window         │ ← 关闭窗口
└──────────────────────┘
```

### 主窗口
- 应用说明和使用指南
- 快捷按钮创建各层级窗口
- 关闭时隐藏到托盘

## 技术实现

- **.NET 8.0** - 目标框架
- **WPF** - UI框架  
- **Windows Forms** - 系统托盘支持 (NotifyIcon)
- **Windows API** - 窗口层级管理 (SetWindowPos, HWND_BOTTOM)
- **DispatcherTimer** - 底层窗口位置维护

### 统一设计架构

使用单一 `FloatingWindow` 组件替代原来的分离设计:
- ✅ 代码复用性高
- ✅ 维护成本低
- ✅ 用户体验一致
- ✅ 运行时动态切换层级
- ✅ 统一的外观和行为

## 使用方法

### 创建窗口
1. 右键系统托盘图标
2. 选择 "新建窗口" → 选择层级
3. 或双击托盘图标打开主窗口，使用按钮创建

### 切换窗口层级
1. 在任何浮动窗口上点击右键
2. 选择 "Window Level" → 选择新的层级
3. 窗口边框颜色和标题会自动更新

### 编辑便签内容
- 直接在窗口中输入文字
- 支持多行文本和换行
- 可调整窗口大小

### 移动和调整
- 拖动窗口任意位置移动
- 拖动窗口四角调整大小

### 关闭窗口
- 点击右上角 ✕ 按钮
- 或右键菜单 → Close Window

## 项目结构

```
YASN/
├── App.xaml                    # 应用程序定义
├── App.xaml.cs                 # 托盘图标管理
├── MainWindow.xaml             # 主窗口UI
├── MainWindow.xaml.cs          # 主窗口逻辑
├── FloatingWindow.xaml         # 统一浮动窗口UI (主要组件)
├── FloatingWindow.xaml.cs      # 浮动窗口逻辑和层级管理
├── DESIGN.md                   # 设计系统说明
├── QUICKSTART.md               # 快速参考指南
└── README.md                   # 项目文档
```

## 依赖包

- System.Drawing.Common (9.0.10) - 用于系统图标支持

## 构建和运行

```bash
dotnet build
dotnet run
```

或在Visual Studio中直接按F5运行。

## 设计亮点

### 配色方案
基于 Flat UI Colors 设计系统:
- **Peter River** (#3498DB) - 顶层蓝色
- **Concrete** (#95A5A6) - 正常灰色  
- **Alizarin** (#E74C3C) - 底层红色
- **Midnight Blue** (#2C3E50) - 深色背景

### 视觉一致性
- 所有窗口相同的标题栏和背景
- 只通过边框颜色和图标区分
- 统一的圆角和阴影效果

### 交互体验
- 关闭按钮悬停红色反馈
- 右键菜单深色主题
- 平滑的颜色过渡

## 常见问题

### Q: 窗口内容会保存吗？
A: 当前版本不支持自动保存。关闭窗口后内容会丢失。

### Q: 如何改变窗口层级？
A: 右键点击窗口，选择 "Window Level"，然后选择新的层级。窗口会立即应用新的层级并更新外观。

### Q: 底层窗口看不见了？
A: 底层窗口被所有其他窗口覆盖是正常的。最小化其他窗口即可看到。

### Q: 可以同时打开多个窗口吗？
A: 可以！每个窗口都可以独立设置层级。

### Q: 如何完全退出？
A: 右键托盘图标，选择 "退出"。

## 未来改进

- [ ] 便签内容自动保存和恢复
- [ ] 富文本支持（粗体、斜体、颜色）
- [ ] 自定义窗口颜色主题
- [ ] 透明度调节
- [ ] 窗口位置记忆
- [ ] 快捷键支持
- [ ] 搜索功能
- [ ] 标签分类系统

## 许可证

[根据需要添加许可证信息]

## 贡献

欢迎提交问题报告和功能建议！

---

**享受使用 YASN！** 🎉
