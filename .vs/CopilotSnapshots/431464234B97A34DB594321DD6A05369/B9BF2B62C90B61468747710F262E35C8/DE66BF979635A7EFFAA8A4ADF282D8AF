# YASN - Yet Another Sticky Notes (Window Manager)

一个基于WPF的系统托盘应用程序，可以创建始终在顶层或底层的无边框窗口。

## 功能特性

### 系统托盘
- 应用启动后会最小化到系统托盘
- 右键托盘图标显示菜单：
  - **Create Top Window** - 创建始终在最顶层的无边框窗口
  - **Create Bottom Window** - 创建始终在最底层的无边框窗口
  - **Exit** - 退出应用程序
- 双击托盘图标显示主窗口

### 顶层窗口 (Top Most Window)
- 始终显示在所有其他窗口之上
- 无边框半透明设计
- 蓝色主题
- 可拖动移动
- 可调整大小
- 点击右上角 X 按钮关闭

### 底层窗口 (Bottom Most Window)
- 始终显示在所有其他窗口之下
- 无边框半透明设计
- 红色主题
- 可拖动移动
- 可调整大小
- 点击右上角 X 按钮关闭
- 使用Windows API确保窗口始终保持在底层

### 主窗口
- 提供应用说明
- 快捷按钮创建顶层/底层窗口
- 关闭时隐藏到托盘而不是退出

## 技术实现

- **.NET 8.0** - 目标框架
- **WPF** - UI框架
- **Windows Forms** - 系统托盘支持 (NotifyIcon)
- **Windows API** - 底层窗口实现 (SetWindowPos)

## 使用方法

1. 运行应用程序
2. 应用会最小化到系统托盘（在任务栏右下角）
3. 右键点击托盘图标选择创建窗口类型
4. 双击托盘图标显示主窗口

## 项目结构

```
YASN/
├── App.xaml                    # 应用程序定义
├── App.xaml.cs                 # 托盘图标管理
├── MainWindow.xaml             # 主窗口UI
├── MainWindow.xaml.cs          # 主窗口逻辑
├── TopMostWindow.xaml          # 顶层窗口UI
├── TopMostWindow.xaml.cs       # 顶层窗口逻辑
├── BottomMostWindow.xaml       # 底层窗口UI
├── BottomMostWindow.xaml.cs    # 底层窗口逻辑（包含Windows API调用）
└── YASN.csproj                 # 项目文件
```

## 依赖包

- System.Drawing.Common (9.0.10) - 用于系统图标支持

## 构建和运行

```bash
dotnet build
dotnet run
```

或在Visual Studio中直接按F5运行。
